fuente:
  tipo: "ventana"                 # "video" | "pantalla" | "ventana"
  titulo_ventana: "Euro Truck Simulator 2"
  ruta_video: "datos/videos/ets2_volvo_fh16.f299.mp4"
  monitor: 0
  region: null
  escalar_a: [1920, 1080]         # redimensiona a 1920x1080 para coincidir con ROI calibradas

modelo:
  pesos: "datos/modelos/yolo11n.pt"
  imgsz: 640
  conf_min: 0.35
  device: "cuda"

control:
  tipo: "nulo"                    # "nulo" | "gamepad" | "teclado"

seguridad:
  tecla_paro: "f12"
  timeout_watchdog_ms: 5000

registro:
  ruta_base: "datos/evidencia"
  grabar_video: true
  fps_objetivo: 30
